---
phase: 03-test-infrastructure-error-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - test/helpers/setup.ts
autonomous: true
requirements:
  - TST-02
must_haves:
  truths:
    - "`better-sqlite3` is listed in `devDependencies`"
    - "`test/helpers/setup.ts` creates an in-memory SQLite database and Drizzle client"
    - "`npm run test` boots without requiring `TURSO_CONNECTION_URL` or `TURSO_AUTH_TOKEN`"
  artifacts:
    - path: "test/helpers/setup.ts"
      provides: "Test database setup with in-memory SQLite"
      min_lines: 20
    - path: "package.json"
      provides: "Updated devDependencies and jest config"
      contains: "better-sqlite3"
  key_links:
    - from: "package.json"
      to: "test/helpers/setup.ts"
      via: "jest setupFilesAfterEnv or globalSetup"
      pattern: "test/helpers/setup"
---

<objective>
Configure Jest to use an in-memory SQLite database for tests, eliminating the need for real Turso credentials during test runs.

Purpose: Enable fast, isolated test execution without network dependencies or database state leakage.
Output: Updated `package.json` (new dependency + jest config), new `test/helpers/setup.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-test-infrastructure-error-resilience/03-CONTEXT.md

Current package.json jest config:
```json
"jest": {
  "moduleFileExtensions": ["js", "json", "ts"],
  "rootDir": "src",
  "testRegex": ".*\\.spec\\.ts$",
  "transform": { "^.+\\.(t|j)s$": "ts-jest" },
  "collectCoverageFrom": ["**/*.(t|j)s"],
  "coverageDirectory": "../coverage",
  "testEnvironment": "node"
}
```

Current test/jest-e2e.json:
```json
{
  "moduleFileExtensions": ["js", "json", "ts"],
  "rootDir": ".",
  "testEnvironment": "node",
  "testRegex": ".e2e-spec.ts$",
  "transform": { "^.+\\.(t|j)s$": "ts-jest" }
}
```

From src/db/schema.ts:
```typescript
import { sqliteTable, text, integer, index, uniqueIndex } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';
// ... schema definitions for user, session, account, verification, transactions, categories
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install better-sqlite3 and update package.json jest config</name>
  <files>package.json</files>
  <action>
    1. Install `better-sqlite3` as a devDependency:
       ```bash
       npm install -D better-sqlite3
       ```
       (If this fails due to native compilation issues, also install `@types/better-sqlite3` if available, but usually better-sqlite3 includes its own types.)
    
    2. Update the `jest` configuration in `package.json` to include `setupFilesAfterEnv` pointing to the test setup file:
       ```json
       "jest": {
         "moduleFileExtensions": ["js", "json", "ts"],
         "rootDir": "src",
         "testRegex": ".*\\.spec\\.ts$",
         "transform": { "^.+\\.(t|j)s$": "ts-jest" },
         "collectCoverageFrom": ["**/*.(t|j)s"],
         "coverageDirectory": "../coverage",
         "testEnvironment": "node",
         "setupFilesAfterEnv": ["<rootDir>/../test/helpers/setup.ts"]
       }
       ```
       
    3. Ensure the e2e config `test/jest-e2e.json` also uses the setup file if needed. Since e2e tests use `rootDir: "."`, adjust the path:
       ```json
       {
         "moduleFileExtensions": ["js", "json", "ts"],
         "rootDir": ".",
         "testEnvironment": "node",
         "testRegex": ".e2e-spec.ts$",
         "transform": { "^.+\\.(t|j)s$": "ts-jest" },
         "setupFilesAfterEnv": ["./helpers/setup.ts"]
       }
       ```
  </action>
  <verify>
    <automated>grep -q "better-sqlite3" package.json && grep -q "setupFilesAfterEnv" package.json && grep -q "test/helpers/setup.ts" package.json</automated>
  </verify>
  <done>`better-sqlite3` is in `devDependencies` and jest config includes `setupFilesAfterEnv`</done>
</task>

<task type="auto">
  <name>Task 2: Create test/helpers/setup.ts with in-memory SQLite</name>
  <files>test/helpers/setup.ts</files>
  <action>
    Create `test/helpers/setup.ts` that sets up an in-memory SQLite database using `better-sqlite3` and Drizzle ORM.
    
    ```typescript
    import Database from 'better-sqlite3';
    import { drizzle } from 'drizzle-orm/better-sqlite3';
    import * as schema from '../../src/db/schema';

    // Create an in-memory SQLite database for tests
    const sqlite = new Database(':memory:');
    
    // Initialize Drizzle with the schema
    export const testDb = drizzle(sqlite, { schema });

    // Optionally: run a simple query to verify the database is working
    // and create tables if needed. Since Drizzle with better-sqlite3
    // doesn't auto-create tables, you may need to push the schema.
    // For now, just export the testDb for use in tests.
    ```
    
    IMPORTANT: The setup file must NOT throw if Turso env vars are missing. The entire purpose is to decouple tests from real database credentials.
    
    NOTE: If `drizzle-orm/better-sqlite3` requires table creation, you may need to add a helper function that creates tables from the schema. However, for the Phase 3 success criteria, simply having the setup file and jest config is sufficient — the actual table creation strategy will be refined in Phase 4 when tests are written.
  </action>
  <verify>
    <automated>test -f test/helpers/setup.ts && grep -q "better-sqlite3" test/helpers/setup.ts && grep -q ":memory:" test/helpers/setup.ts && grep -q "drizzle-orm/better-sqlite3" test/helpers/setup.ts</automated>
  </verify>
  <done>`test/helpers/setup.ts` exists and exports an in-memory SQLite Drizzle client</done>
</task>

<task type="auto">
  <name>Task 3: Verify tests boot without real database credentials</name>
  <files>package.json, test/helpers/setup.ts</files>
  <action>
    Verify that `npm run test` can execute without `TURSO_CONNECTION_URL` or `TURSO_AUTH_TOKEN` being set.
    
    Temporarily unset the env vars and run jest in list mode (which doesn't execute tests but validates config):
    ```bash
    unset TURSO_CONNECTION_URL TURSO_AUTH_TOKEN && npx jest --listTests 2>&1 | head -20
    ```
    
    If the setup file causes errors during jest boot, fix them. Common issues:
    - `better-sqlite3` native module compilation failure → may need `npm rebuild` or ensure Python/node-gyp are available
    - Path resolution issues with `setupFilesAfterEnv` → verify the relative path from `rootDir`
  </action>
  <verify>
    <automated>bash -c 'unset TURSO_CONNECTION_URL TURSO_AUTH_TOKEN; npx jest --listTests --silent 2>&1 | grep -q "spec.ts" || echo "Jest booted successfully"'</automated>
  </verify>
  <done>Jest boots without requiring Turso credentials; `npm run test` does not fail due to missing env vars</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test setup → In-memory DB | Internal test-only boundary; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | test/helpers/setup.ts | accept | In-memory database is destroyed after test process exits; no persistent data |
</threat_model>

<verification>
1. `grep -q "better-sqlite3" package.json` — devDependency present
2. `test -f test/helpers/setup.ts` — setup file exists
3. `grep -q ":memory:" test/helpers/setup.ts` — in-memory database configured
4. `npm run test -- --listTests` succeeds without `TURSO_CONNECTION_URL` set
</verification>

<success_criteria>
- `better-sqlite3` is installed as a devDependency
- `test/helpers/setup.ts` creates an in-memory SQLite database and Drizzle client
- Jest config includes `setupFilesAfterEnv` pointing to the setup file
- `npm run test` boots successfully without Turso credentials
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-infrastructure-error-resilience/03-02-SUMMARY.md`
</output>
