---
phase: 04-automated-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/categories/categories.service.spec.ts
  - src/transactions/transactions.service.spec.ts
  - src/account/account.service.spec.ts
autonomous: true
requirements: [QAL-01]
must_haves:
  truths:
    - CategoriesService unit tests pass with testDb injection
    - TransactionsService unit tests cover CRUD and error paths
    - AccountService unit tests cover delete cascade and export
  artifacts:
    - path: src/categories/categories.service.spec.ts
      provides: CategoriesService unit tests
      min_lines: 80
    - path: src/transactions/transactions.service.spec.ts
      provides: TransactionsService unit tests
      min_lines: 120
    - path: src/account/account.service.spec.ts
      provides: AccountService unit tests
      min_lines: 80
  key_links:
    - from: spec files
      to: test/helpers/setup.ts
      via: testDb imported and provided as DRIZZLE
    - from: spec files
      to: src/db/db-token.ts
      via: DRIZZLE token imported for provider override
---

<objective>
Create unit tests for the three core database-backed services: CategoriesService, TransactionsService, and AccountService.

Purpose: Verify business logic for category lazy-init, transaction CRUD with amount negation, and account data operations.
Output: Three `.spec.ts` files with passing tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-automated-quality-gates/04-CONTEXT.md
@src/categories/categories.service.ts
@src/transactions/transactions.service.ts
@src/account/account.service.ts
@src/db/db-token.ts
@test/helpers/setup.ts
</context>

<interfaces>
From src/db/db-token.ts:
```typescript
export const DRIZZLE = Symbol('DRIZZLE');
export type DrizzleDatabase = LibSQLDatabase<typeof schema>;
```

From test/helpers/setup.ts:
```typescript
import Database from 'better-sqlite3';
import { drizzle } from 'drizzle-orm/better-sqlite3';
import * as schema from '../../src/db/schema';
export const testDb = drizzle(new Database(':memory:'), { schema });
```

Note: `testDb` is `BetterSQLite3Database<typeof schema>` but `DrizzleDatabase` is `LibSQLDatabase<typeof schema>`. Use `useValue: testDb as any` in provider override.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create CategoriesService unit tests</name>
  <files>src/categories/categories.service.spec.ts</files>
  <action>
    Create `src/categories/categories.service.spec.ts` with the following structure:

    1. Imports: `Test, TestingModule` from `@nestjs/testing`; `CategoriesService` from `./categories.service`; `DRIZZLE` from `../db/db-token`; `testDb` from `../../test/helpers/setup`; `categories` from `../db/schema`; `eq` from `drizzle-orm`; `nanoid` from `nanoid`.

    2. In `beforeEach`: create `TestingModule` with providers `[CategoriesService, { provide: DRIZZLE, useValue: testDb as any }]`, compile and get service instance.

    3. In `beforeEach` or `afterEach`: clean the `categories` table via `testDb.delete(categories).where(eq(categories.userId, 'test-user'))` to ensure isolation between tests.

    4. Test cases:
       - `list() with new user — lazy initializes default categories`: Call `service.list('test-user')`. Assert result is an array of 8 items with slugs: `['an-uong', 'di-chuyen', 'mua-sam', 'giai-tri', 'hoa-don', 'suc-khoe', 'giao-duc', 'khac']`. Assert each has `id`, `name`, `slug`, `budget: null`. Call `service.list('test-user')` a second time and assert same 8 items (no duplicates created).
       - `list() with existing categories — returns without creating defaults`: Manually insert 2 categories into `testDb` for `'existing-user'`. Call `service.list('existing-user')`. Assert result has exactly 2 items. Query `testDb.select().from(categories).where(eq(categories.userId, 'existing-user'))` and assert count is still 2 (no defaults injected).

    5. Run `npx jest src/categories/categories.service.spec.ts --no-coverage` and verify it passes.
  </action>
  <verify>
    <automated>npx jest src/categories/categories.service.spec.ts --no-coverage --silent</automated>
  </verify>
  <done>
    - File `src/categories/categories.service.spec.ts` exists and `jest` exits 0
    - Tests cover lazy-init and existing categories paths
    - Database is cleaned between tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TransactionsService unit tests</name>
  <files>src/transactions/transactions.service.spec.ts</files>
  <action>
    Create `src/transactions/transactions.service.spec.ts` with the following structure:

    1. Imports: `Test, TestingModule` from `@nestjs/testing`; `TransactionsService` from `./transactions.service`; `DRIZZLE` from `../db/db-token`; `testDb` from `../../test/helpers/setup`; `transactions` from `../db/schema`; `eq, and` from `drizzle-orm`; `NotFoundException, BadRequestException` from `@nestjs/common`.

    2. In `beforeEach`: create `TestingModule` with providers `[TransactionsService, { provide: DRIZZLE, useValue: testDb as any }]`, compile and get service instance.

    3. Clean `transactions` table between tests: `testDb.delete(transactions).where(eq(transactions.userId, 'test-user'))`.

    4. Helper: `createTestTransaction(userId, overrides)` that inserts a transaction directly into `testDb` with default values (`amount: -35000`, `merchant: 'Cà phê'`, `category: 'Ăn uống'`, `source: 'text'`, `createdAt: '2026-04-15T10:00:00.000Z'`) and merges overrides.

    5. Test cases:
       - `listByMonth() returns transactions for user ordered by createdAt desc`: Create 2 transactions for `'test-user'` with different `createdAt` dates. Call `service.listByMonth('test-user')`. Assert result is array of length 2, ordered with newest first.
       - `listByMonth() filters by month parameter`: Create transactions for April 2026 and March 2026. Call `service.listByMonth('test-user', '2026-04')`. Assert result contains only April transactions.
       - `listByMonth() with invalid month throws BadRequestException`: Call `service.listByMonth('test-user', 'bad-month')`. Assert throws `BadRequestException` with message containing `Invalid month format`.
       - `create() stores negated amount`: Call `service.create('test-user', { amount: 35000, merchant: 'Cà phê', category: 'Ăn uống', source: 'text' })`. Assert result `amount` is `-35000`.
       - `update() modifies transaction`: Create a transaction. Call `service.update('test-user', tx.id, { amount: 50000, merchant: 'Updated' })`. Assert result `amount` is `-50000` and `merchant` is `'Updated'`.
       - `update() not found throws NotFoundException`: Call `service.update('test-user', 'nonexistent-id', { amount: 10000 })`. Assert throws `NotFoundException`.
       - `delete() removes transaction`: Create a transaction. Call `service.delete('test-user', tx.id)`. Assert result is `{ success: true }`. Query `testDb` and verify transaction no longer exists.
       - `delete() not found throws NotFoundException`: Call `service.delete('test-user', 'nonexistent-id')`. Assert throws `NotFoundException`.

    6. Run `npx jest src/transactions/transactions.service.spec.ts --no-coverage` and verify it passes.
  </action>
  <verify>
    <automated>npx jest src/transactions/transactions.service.spec.ts --no-coverage --silent</automated>
  </verify>
  <done>
    - File `src/transactions/transactions.service.spec.ts` exists and `jest` exits 0
    - Tests cover listByMonth filtering, create amount negation, update, delete, and NotFoundException paths
    - Invalid month format throws BadRequestException
  </done>
</task>

<task type="auto">
  <name>Task 3: Create AccountService unit tests</name>
  <files>src/account/account.service.spec.ts</files>
  <action>
    Create `src/account/account.service.spec.ts` with the following structure:

    1. Imports: `Test, TestingModule` from `@nestjs/testing`; `AccountService` from `./account.service`; `DRIZZLE` from `../db/db-token`; `testDb` from `../../test/helpers/setup`; `transactions, categories, user, session, account` from `../db/schema`; `eq` from `drizzle-orm`.

    2. In `beforeEach`: create `TestingModule` with providers `[AccountService, { provide: DRIZZLE, useValue: testDb as any }]`, compile and get service instance.

    3. Clean all relevant tables between tests: delete from `transactions`, `categories`, `session`, `account`, `user` where `userId = 'test-user'`.

    4. Helper: `seedUserData(userId)` that inserts:
       - 1 user record in `user` table with `id: userId`, `name: 'Test'`, `email: 'test@example.com'`
       - 2 transaction records for the user
       - 2 category records for the user
       - 1 session record for the user
       - 1 account record for the user

    5. Test cases:
       - `deleteAccount() removes all user data`: Call `seedUserData('test-user')`, then `service.deleteAccount('test-user')`. Assert result is `{ success: true }`. Query each table (`transactions`, `categories`, `session`, `account`, `user`) and verify no records exist for `'test-user'`.
       - `exportData() returns transactions and categories`: Call `seedUserData('test-user')`, then `service.exportData('test-user')`. Assert result has `exportedAt` (ISO string), `transactions` array of length 2, `categories` array of length 2.
       - `exportData() returns empty arrays for new user`: Call `service.exportData('new-user')`. Assert `transactions` is empty array and `categories` is empty array.

    6. Run `npx jest src/account/account.service.spec.ts --no-coverage` and verify it passes.
  </action>
  <verify>
    <automated>npx jest src/account/account.service.spec.ts --no-coverage --silent</automated>
  </verify>
  <done>
    - File `src/account/account.service.spec.ts` exists and `jest` exits 0
    - deleteAccount test verifies cascade deletion across all 5 tables
    - exportData test returns correct structure with transactions and categories
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test → production db | Tests must never connect to production Turso database |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | testDb singleton | mitigate | `testDb` uses `:memory:` SQLite; no external connections |
| T-04-02 | Information Disclosure | test data leakage | accept | Test data is synthetic; no PII used |
</threat_model>

<verification>
- All three spec files exist and compile
- `npx jest src/categories/categories.service.spec.ts src/transactions/transactions.service.spec.ts src/account/account.service.spec.ts --no-coverage` exits 0
</verification>

<success_criteria>
- 3 service spec files created with comprehensive test coverage
- All tests pass with `testDb` injected as `DRIZZLE`
- Database cleaned between tests to prevent cross-test pollution
- No production database connections in tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-automated-quality-gates/04-01-SUMMARY.md`
</output>
