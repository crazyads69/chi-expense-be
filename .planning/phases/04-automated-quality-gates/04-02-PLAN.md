---
phase: 04-automated-quality-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/input/input.service.spec.ts
  - src/insights/insights.service.spec.ts
  - src/input/rate-limit.guard.spec.ts
autonomous: true
requirements: [QAL-01]
must_haves:
  truths:
    - InputService unit tests pass with mocked OpenAI client
    - InsightsService unit tests pass with testDb injection
    - RateLimitGuard unit tests pass with mocked Redis client
  artifacts:
    - path: src/input/input.service.spec.ts
      provides: InputService unit tests
      min_lines: 150
    - path: src/insights/insights.service.spec.ts
      provides: InsightsService unit tests
      min_lines: 80
    - path: src/input/rate-limit.guard.spec.ts
      provides: RateLimitGuard unit tests
      min_lines: 60
  key_links:
    - from: src/input/input.service.spec.ts
      to: src/lib/openrouter.ts
      via: OpenAI client mocked
    - from: src/input/rate-limit.guard.spec.ts
      to: src/lib/redis.ts
      via: getRatelimitClient mocked
---

<objective>
Create unit tests for InputService (with mocked LLM), InsightsService (with testDb), and RateLimitGuard (with mocked Redis).

Purpose: Verify LLM parsing logic, insights aggregation, and rate limiting behavior without external API dependencies.
Output: Three `.spec.ts` files with passing tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-automated-quality-gates/04-CONTEXT.md
@src/input/input.service.ts
@src/insights/insights.service.ts
@src/input/rate-limit.guard.ts
@src/lib/openrouter.ts
@src/lib/redis.ts
@test/helpers/setup.ts
</context>

<interfaces>
From src/lib/openrouter.ts:
```typescript
export const getOpenAIClient = () => {
  return new OpenAI({ baseURL: 'https://openrouter.ai/api/v1', apiKey: process.env.OPENROUTER_API_KEY, timeout: 8000, maxRetries: 1 });
};
```

From src/lib/redis.ts:
```typescript
export const getRatelimitClient = () => {
  if (!ratelimitClient) {
    ratelimitClient = new Ratelimit({ redis: getRedisClient(), limiter: Ratelimit.slidingWindow(20, '1 h'), analytics: true });
  }
  return ratelimitClient;
};
```

From src/input/input.service.ts:
```typescript
export interface ParsedExpense { amount: number; merchant: string; category: string; note?: string; }
```

Mock pattern for OpenAI:
```typescript
jest.mock('../lib/openrouter', () => ({
  getOpenAIClient: jest.fn(() => ({
    chat: { completions: { create: jest.fn() } }
  }))
}));
```

Mock pattern for Redis:
```typescript
jest.mock('../lib/redis', () => ({
  getRatelimitClient: jest.fn(() => ({
    limit: jest.fn()
  }))
}));
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create InputService unit tests</name>
  <files>src/input/input.service.spec.ts</files>
  <action>
    Create `src/input/input.service.spec.ts` with the following structure:

    1. Mock `../lib/openrouter` module at the top of the file using `jest.mock('../lib/openrouter', () => ({ getOpenAIClient: jest.fn() }))`.

    2. Import `InputService` from `./input.service`; `HttpException, HttpStatus` from `@nestjs/common`.

    3. Create a helper `mockOpenAIResponse(content: string)` that sets up `getOpenAIClient` to return `{ chat: { completions: { create: jest.fn().mockResolvedValue({ choices: [{ message: { content } }] }) } } }`.

    4. In `beforeEach`: reset all mocks; instantiate `InputService` directly (it has no constructor dependencies).

    5. Test cases for `parseText()`:
       - `with merchant map match — returns local parse without LLM`: Call `service.parseText('user-1', 'grab 35k')`. Assert result: `{ amount: 35000, merchant: 'Grab', category: 'Di chuyển' }` (merchant may vary based on `extractMerchant` logic, but category must be `'Di chuyển'` and amount `35000`).
       - `with LLM success — returns parsed JSON`: Call `mockOpenAIResponse('{"amount": 50000, "merchant": "Cà phê sữa", "category": "Ăn uống"}')`. Call `service.parseText('user-1', 'some message')`. Assert result `amount` is `50000`, `merchant` is `'Cà phê sữa'`, `category` is `'Ăn uống'`.
       - `with LLM missing JSON — throws 502`: Call `mockOpenAIResponse('No JSON here')`. Call `service.parseText('user-1', 'test')`. Assert throws `HttpException` with `getStatus() === 502`.
       - `with LLM invalid JSON — throws 502`: Call `mockOpenAIResponse('{invalid}')`. Call `service.parseText('user-1', 'test')`. Assert throws `HttpException` with `getStatus() === 502`.
       - `with OpenAI API error — throws 503`: Call `mockOpenAIResponse` to throw `new Error('timeout')`. Call `service.parseText('user-1', 'test')`. Assert throws `HttpException` with `getStatus() === 503` and message containing `taking too long` or `temporarily unavailable`.
       - `with LLM partial JSON — falls back to local extraction`: Call `mockOpenAIResponse('{"amount": 100000}')`. Call `service.parseText('user-1', 'test')`. Assert `amount` is `100000`, `category` is `'Khác'` (fallback), merchant is not `'Unknown'` (should use local extraction fallback).

    6. Test cases for `parseImage()`:
       - `with LLM success — returns parsed JSON`: Same mock setup as text. Call `service.parseImage('user-1', 'data:image/jpeg;base64,abc123')`. Assert result has `amount`, `merchant`, `category`.
       - `with LLM API error — throws 503`: Mock API error. Call `service.parseImage('user-1', 'data:image/jpeg;base64,abc123')`. Assert throws `HttpException` with `getStatus() === 503`.

    7. Run `npx jest src/input/input.service.spec.ts --no-coverage` and verify it passes.
  </action>
  <verify>
    <automated>npx jest src/input/input.service.spec.ts --no-coverage --silent</automated>
  </verify>
  <done>
    - File `src/input/input.service.spec.ts` exists and `jest` exits 0
    - Tests cover merchant map fallback, LLM success, missing JSON, invalid JSON, API timeout/error
    - parseImage tests cover success and error paths
    - OpenAI client is fully mocked — no real API calls
  </done>
</task>

<task type="auto">
  <name>Task 2: Create InsightsService unit tests</name>
  <files>src/insights/insights.service.spec.ts</files>
  <action>
    Create `src/insights/insights.service.spec.ts` with the following structure:

    1. Imports: `Test, TestingModule` from `@nestjs/testing`; `InsightsService` from `./insights.service`; `DRIZZLE` from `../db/db-token`; `testDb` from `../../test/helpers/setup`; `transactions` from `../db/schema`; `eq` from `drizzle-orm`; `BadRequestException` from `@nestjs/common`.

    2. In `beforeEach`: create `TestingModule` with providers `[InsightsService, { provide: DRIZZLE, useValue: testDb as any }]`, compile and get service instance.

    3. Clean `transactions` table between tests: `testDb.delete(transactions).where(eq(transactions.userId, 'test-user'))`.

    4. Helper: `createTransaction(userId, amount, category, createdAt)` that inserts a transaction with the given values. Amount should be negative for expenses (e.g., `-35000`).

    5. Test cases:
       - `getMonthlyInsights() returns total, category breakdown, and daily expenses`: Create 3 transactions for `'test-user'` in April 2026: `(-35000, 'Ăn uống', '2026-04-10T10:00:00.000Z')`, `(-50000, 'Di chuyển', '2026-04-12T10:00:00.000Z')`, `(-35000, 'Ăn uống', '2026-04-15T10:00:00.000Z')`. Call `service.getMonthlyInsights('test-user', '2026-04')`. Assert: `total` is `120000`, `transactionCount` is `3`, `categoryBreakdown` has 2 entries (`Ăn uống` with `total: 70000, count: 2`, `Di chuyển` with `total: 50000, count: 1`), `dailyExpenses` has 3 entries sorted by date.
       - `getMonthlyInsights() with no data returns zeros`: Call `service.getMonthlyInsights('test-user', '2026-04')`. Assert: `total` is `0`, `transactionCount` is `0`, `categoryBreakdown` is empty array, `dailyExpenses` is empty array.
       - `getMonthlyInsights() with invalid month throws BadRequestException`: Call `service.getMonthlyInsights('test-user', 'bad-month')`. Assert throws `BadRequestException`.

    6. Run `npx jest src/insights/insights.service.spec.ts --no-coverage` and verify it passes.
  </action>
  <verify>
    <automated>npx jest src/insights/insights.service.spec.ts --no-coverage --silent</automated>
  </verify>
  <done>
    - File `src/insights/insights.service.spec.ts` exists and `jest` exits 0
    - Tests cover aggregation with multiple categories and dates
    - Empty month returns zero totals and empty arrays
    - Invalid month throws BadRequestException
  </done>
</task>

<task type="auto">
  <name>Task 3: Create RateLimitGuard unit tests</name>
  <files>src/input/rate-limit.guard.spec.ts</files>
  <action>
    Create `src/input/rate-limit.guard.spec.ts` with the following structure:

    1. Mock `../lib/redis` module at the top:
    ```typescript
    jest.mock('../lib/redis', () => ({
      getRatelimitClient: jest.fn(() => ({
        limit: jest.fn()
      }))
    }));
    ```

    2. Imports: `RateLimitGuard` from `./rate-limit.guard`; `ExecutionContext` from `@nestjs/common`; `getRatelimitClient` from `../lib/redis`; `HttpException, HttpStatus` from `@nestjs/common`.

    3. Helper `createMockExecutionContext(user?: { id: string })` that returns a mock `ExecutionContext` with `switchToHttp().getRequest()` returning `{ user }`.

    4. In `beforeEach`: reset `getRatelimitClient` mock.

    5. Test cases:
       - `canActivate() with authenticated user and success returns true`: Setup `getRatelimitClient` mock so `limit()` resolves to `{ success: true }`. Create context with `user: { id: 'user-1' }`. Call `guard.canActivate(context)`. Assert result is `true`. Assert `limit` was called with `'ratelimit_user-1'`.
       - `canActivate() with no user throws 401`: Create context with no user. Call `guard.canActivate(context)`. Assert throws `HttpException` with `getStatus() === 401` and message `'Authentication required'`.
       - `canActivate() with rate limit exceeded throws 429`: Setup `limit()` to resolve `{ success: false }`. Create context with `user: { id: 'user-1' }`. Call `guard.canActivate(context)`. Assert throws `HttpException` with `getStatus() === 429` and message `'Rate limit exceeded'`.

    6. Run `npx jest src/input/rate-limit.guard.spec.ts --no-coverage` and verify it passes.
  </action>
  <verify>
    <automated>npx jest src/input/rate-limit.guard.spec.ts --no-coverage --silent</automated>
  </verify>
  <done>
    - File `src/input/rate-limit.guard.spec.ts` exists and `jest` exits 0
    - Tests cover authenticated success, unauthenticated 401, rate limited 429
    - Redis client is fully mocked — no real Redis connections
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test → OpenRouter API | Tests must never call real OpenRouter API |
| test → Redis | Tests must never call real Upstash Redis |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | OpenAI API called from tests | mitigate | `jest.mock('../lib/openrouter')` prevents all real API calls |
| T-04-04 | Information Disclosure | API keys in test logs | accept | No API keys in test code; env vars not loaded in test environment |
</threat_model>

<verification>
- All three spec files exist and compile
- `npx jest src/input/input.service.spec.ts src/insights/insights.service.spec.ts src/input/rate-limit.guard.spec.ts --no-coverage` exits 0
</verification>

<success_criteria>
- InputService tests pass with fully mocked OpenAI client
- InsightsService tests pass with testDb injection
- RateLimitGuard tests pass with fully mocked Redis client
- No external API calls during test execution
</success_criteria>

<output>
After completion, create `.planning/phases/04-automated-quality-gates/04-02-SUMMARY.md`
</output>
