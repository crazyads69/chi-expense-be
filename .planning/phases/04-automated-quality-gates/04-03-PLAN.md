---
phase: 04-automated-quality-gates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/prompts.spec.ts
  - src/input/input.service.spec.ts
autonomous: true
requirements: [QAL-02]
must_haves:
  truths:
    - parseAmount correctly handles Vietnamese amount formats
    - lookupMerchant matches keywords to categories
    - Prompt formatting wraps user messages correctly
    - sanitizeUserMessage strips injection characters
  artifacts:
    - path: src/lib/prompts.spec.ts
      provides: Prompt and merchant table unit tests
      min_lines: 50
    - path: src/input/input.service.spec.ts
      provides: InputService utility method tests (appended to service tests)
      min_lines: 200
  key_links:
    - from: src/lib/prompts.spec.ts
      to: src/lib/prompts.ts
      via: imports and tests exported functions
    - from: src/input/input.service.spec.ts
      to: src/input/input.service.ts
      via: tests private methods via (service as any)
---

<objective>
Create unit tests for utility functions: Vietnamese amount parsing, merchant keyword lookup, prompt formatting, and input sanitization.

Purpose: Verify edge cases in text processing logic that are critical for LLM pre-processing and fallback extraction.
Output: `src/lib/prompts.spec.ts` and extended `src/input/input.service.spec.ts` with utility tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-automated-quality-gates/04-CONTEXT.md
@src/input/input.service.ts
@src/lib/prompts.ts
@src/lib/merchant-table.ts
</context>

<interfaces>
From src/input/input.service.ts (private methods):
```typescript
private parseAmount(message: string): number
private lookupMerchant(message: string): string | null
private extractMerchant(message: string): string
private sanitizeUserMessage(message: string): string
```

From src/lib/prompts.ts:
```typescript
export const SYSTEM_PROMPT = `...`;
export function USER_PROMPT_TEMPLATE(message: string): string
```

From src/lib/merchant-table.ts:
```typescript
export const MERCHANT_CATEGORY_MAP: Map<string, string>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create InputService utility method tests</name>
  <files>src/input/input.service.spec.ts</files>
  <action>
    Extend `src/input/input.service.spec.ts` (created in plan 04-02) with a `describe('Utility methods')` block that tests the private methods via `(service as any).methodName()`:

    1. `parseAmount()` tests:
       - `'35k' → 35000`
       - `'50 nghìn' → 50000`
       - `'100.5k' → 100500`
       - `'100,000' → 100000`
       - `'200,000 vnd' → 200000`
       - `'abc' → 0`
       - `'0' → 0`
       - Empty string → 0
       - String > 500 chars → 0 (MAX_MESSAGE_LENGTH guard)

    2. `lookupMerchant()` tests:
       - `'đi grab' → 'Di chuyển'`
       - `'uống cà phê' → 'Ăn uống'`
       - `'mua sách' → null` (unless 'sách' is in map — check merchant-table.ts)
       - Case insensitivity: `'GRAB' → 'Di chuyển'`
       - `'nothing' → null`

    3. `extractMerchant()` tests:
       - `'cà phê 35k' → 'cà phê'`
       - `'Xăng 50 nghìn đồng' → 'Xăng'`
       - `'12345' → 'Unknown'`
       - Truncation: string > 50 chars → first 50 chars

    4. `sanitizeUserMessage()` tests:
       - `'hello {world}' → 'hello world'` (braces stripped)
       - `'test [1, 2]' → 'test 1, 2'` (brackets stripped)
       - `'code: `value`' → 'code: value'` (backticks stripped)
       - `'"""quoted"""' → '"quoted"'` (triple quotes replaced)
       - Preserves Vietnamese: `'cà phê sữa đá' → 'cà phê sữa đá'`
       - Strips control chars: `'test\x00\x01' → 'test'`

    5. Append these tests to the existing `src/input/input.service.spec.ts` file. Use `describe('parseText utility methods', () => { ... })` as the wrapper. Access private methods with `(service as any).parseAmount('35k')`.

    6. Run `npx jest src/input/input.service.spec.ts --no-coverage` and verify all tests pass.
  </action>
  <verify>
    <automated>npx jest src/input/input.service.spec.ts --no-coverage --silent</automated>
  </verify>
  <done>
    - `src/input/input.service.spec.ts` contains utility method test section
    - parseAmount covers Vietnamese formats, comma separators, edge cases
    - lookupMerchant covers keyword matching and case insensitivity
    - extractMerchant removes amounts and currency words
    - sanitizeUserMessage strips JSON/control chars while preserving Vietnamese
  </done>
</task>

<task type="auto">
  <name>Task 2: Create prompt and merchant table tests</name>
  <files>src/lib/prompts.spec.ts</files>
  <action>
    Create `src/lib/prompts.spec.ts` with the following tests:

    1. Imports: `SYSTEM_PROMPT, USER_PROMPT_TEMPLATE` from `./prompts`; `MERCHANT_CATEGORY_MAP` from `./merchant-table`.

    2. `USER_PROMPT_TEMPLATE()` tests:
       - `wraps message in delimiters`: Call `USER_PROMPT_TEMPLATE('cà phê 35k')`. Assert result contains `<<<USER_MESSAGE>>>`, `cà phê 35k`, and `<<<END_USER_MESSAGE>>>`.
       - `handles empty string`: Call `USER_PROMPT_TEMPLATE('')`. Assert result contains empty delimiter block.
       - `preserves Vietnamese`: Call `USER_PROMPT_TEMPLATE('đồ ăn vặt 20k')`. Assert result contains the exact Vietnamese text.

    3. `SYSTEM_PROMPT` tests:
       - `is non-empty string`: Assert `typeof SYSTEM_PROMPT === 'string'` and length > 100.
       - `contains JSON instruction`: Assert `SYSTEM_PROMPT` contains `'JSON'`.
       - `contains category list`: Assert `SYSTEM_PROMPT` contains `'Ăn uống'` and `'Di chuyển'`.

    4. `MERCHANT_CATEGORY_MAP` tests:
       - `has expected categories`: Assert `MERCHANT_CATEGORY_MAP.get('grab') === 'Di chuyển'`.
       - `has expected food merchants`: Assert `MERCHANT_CATEGORY_MAP.get('cà phê') === 'Ăn uống'`.
       - `is case-sensitive map`: Verify that `MERCHANT_CATEGORY_MAP.get('GRAB')` is `undefined` (the lookup does case-insensitive matching in service, but the map itself is case-sensitive).
       - `has no empty keys`: Iterate map entries and assert every key is non-empty string.

    5. Run `npx jest src/lib/prompts.spec.ts --no-coverage` and verify it passes.
  </action>
  <verify>
    <automated>npx jest src/lib/prompts.spec.ts --no-coverage --silent</automated>
  </verify>
  <done>
    - File `src/lib/prompts.spec.ts` exists and `jest` exits 0
    - USER_PROMPT_TEMPLATE wraps messages in correct delimiters
    - SYSTEM_PROMPT contains required instructions and categories
    - MERCHANT_CATEGORY_MAP has expected entries
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user input → LLM | sanitizeUserMessage is the sanitization boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | parseAmount regex | mitigate | Tests verify regex handles edge cases (commas, decimals, suffixes) |
| T-04-06 | Spoofing | MERCHANT_CATEGORY_MAP | accept | Map is static data; no user input affects map contents |
</threat_model>

<verification>
- `npx jest src/input/input.service.spec.ts src/lib/prompts.spec.ts --no-coverage` exits 0
</verification>

<success_criteria>
- parseAmount handles all documented Vietnamese amount formats
- lookupMerchant correctly maps keywords to categories
- USER_PROMPT_TEMPLATE wraps messages in `<<<USER_MESSAGE>>>` delimiters
- sanitizeUserMessage strips JSON/control characters while preserving Vietnamese text
- All utility tests pass without external dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/04-automated-quality-gates/04-03-SUMMARY.md`
</output>
