---
phase: 04-automated-quality-gates
plan: 04
type: execute
wave: 2
depends_on: ['04-01', '04-02', '04-03']
files_modified:
  - test/helpers/e2e-app.ts
  - test/app.e2e-spec.ts
autonomous: true
requirements: [QAL-03]
must_haves:
  truths:
    - E2E test helper creates app with testDb and mock auth
    - All API endpoints have happy-path E2E scenarios
    - E2E tests run with npm run test:e2e and pass
  artifacts:
    - path: test/helpers/e2e-app.ts
      provides: E2E test app factory with mock session
      min_lines: 40
    - path: test/app.e2e-spec.ts
      provides: Comprehensive E2E scenarios
      min_lines: 200
  key_links:
    - from: test/helpers/e2e-app.ts
      to: src/app.module.ts
      via: imports AppModule and overrides DRIZZLE + AuthGuard
    - from: test/app.e2e-spec.ts
      to: test/helpers/e2e-app.ts
      via: imports createTestApp helper
---

<objective>
Create end-to-end tests covering all API endpoints with happy-path scenarios using a test database and mock authentication.

Purpose: Verify the full request/response cycle for every endpoint, including database persistence and DTO validation.
Output: `test/helpers/e2e-app.ts` and `test/app.e2e-spec.ts` with passing E2E tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-automated-quality-gates/04-CONTEXT.md
@src/app.module.ts
@src/db/db-token.ts
@test/helpers/setup.ts
@src/health.controller.ts
</context>

<interfaces>
From src/db/db-token.ts:
```typescript
export const DRIZZLE = Symbol('DRIZZLE');
export type DrizzleDatabase = LibSQLDatabase<typeof schema>;
```

From test/helpers/setup.ts:
```typescript
export const testDb = drizzle(new Database(':memory:'), { schema });
```

AuthGuard override pattern:
```typescript
import { AuthGuard } from '@thallesp/nestjs-better-auth';

const moduleRef = await Test.createTestingModule({ imports: [AppModule] })
  .overrideProvider(DRIZZLE)
  .useValue(testDb as any)
  .overrideGuard(AuthGuard)
  .useValue({
    canActivate: (context: ExecutionContext) => {
      const req = context.switchToHttp().getRequest();
      req.session = { user: { id: 'e2e-test-user' } };
      return true;
    },
  })
  .compile();
```

The `@Session()` decorator returns `request.session`. The overridden guard sets `req.session` before the decorator reads it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test infrastructure</name>
  <files>test/helpers/e2e-app.ts</files>
  <action>
    Create `test/helpers/e2e-app.ts` with the following content:

    1. Imports:
       - `Test, TestingModule` from `@nestjs/testing`
       - `INestApplication` from `@nestjs/common`
       - `AppModule` from `../../src/app.module`
       - `DRIZZLE` from `../../src/db/db-token`
       - `testDb` from `./setup`
       - `AuthGuard` from `@thallesp/nestjs-better-auth`

    2. Export async function `createTestApp(): Promise<INestApplication>` that:
       - Creates `TestingModule` with `imports: [AppModule]`
       - Overrides `DRIZZLE` provider: `.overrideProvider(DRIZZLE).useValue(testDb as any)`
       - Overrides `AuthGuard`: `.overrideGuard(AuthGuard).useValue({ canActivate: (context) => { const req = context.switchToHttp().getRequest(); req.session = { user: { id: 'e2e-test-user' } }; return true; } })`
       - Compiles the module
       - Creates NestJS app: `moduleRef.createNestApplication()`
       - Initializes app: `await app.init()`
       - Returns the app

    3. Export async function `closeTestApp(app: INestApplication)` that calls `await app.close()`.

    4. Export function `cleanTestData()` that deletes all records from `transactions`, `categories`, `user`, `session`, `account` tables in `testDb` where `userId` matches `'e2e-test-user'`. Use `testDb.delete(table).where(eq(table.userId, 'e2e-test-user'))` for each table. For the `user` table, match on `id` instead of `userId`.

    5. Verify the file compiles by running `npx tsc --noEmit test/helpers/e2e-app.ts` (may need to use project's tsconfig).
  </action>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck test/helpers/e2e-app.ts 2>&1 | grep -c "error TS" | grep -q "^0$"</automated>
  </verify>
  <done>
    - `test/helpers/e2e-app.ts` exists and TypeScript compiles without errors
    - `createTestApp()` returns initialized NestJS app with testDb and mock auth
    - `cleanTestData()` removes all test records from relevant tables
  </done>
</task>

<task type="auto">
  <name>Task 2: Write comprehensive E2E scenarios</name>
  <files>test/app.e2e-spec.ts</files>
  <action>
    Rewrite `test/app.e2e-spec.ts` with comprehensive E2E scenarios. Replace the existing stub content entirely.

    1. Imports:
       - `Test, INestApplication, HttpStatus` from `@nestjs/common`
       - `request` from `supertest`
       - `App` from `supertest/types`
       - `createTestApp, closeTestApp, cleanTestData` from `./helpers/e2e-app`
       - `testDb` from `./helpers/setup`
       - `transactions, categories` from `../src/db/schema`
       - `eq` from `drizzle-orm`

    2. Test setup:
       - `let app: INestApplication<App>;`
       - `beforeAll(async () => { app = await createTestApp(); });`
       - `afterAll(async () => { await closeTestApp(app); });`
       - `beforeEach(async () => { await cleanTestData(); });`

    3. Test scenarios (use `describe` blocks for organization):

       **Health Endpoint:**
       - `GET /api/health` → expect 200, body contains `{ status: 'ok' }` and `timestamp`.

       **Categories Endpoint:**
       - `GET /api/categories` → expect 200, body is array of 8 default categories with slugs `['an-uong', 'di-chuyen', 'mua-sam', 'giai-tri', 'hoa-don', 'suc-khoe', 'giao-duc', 'khac']`.

       **Transactions Endpoints:**
       - `POST /api/transactions` → send `{ amount: 35000, merchant: 'Cà phê', category: 'Ăn uống', source: 'text' }`. Expect 201 or 200, body contains `id`, `amount: -35000`, `merchant: 'Cà phê'`. Capture `transactionId` from response.
       - `GET /api/transactions` → expect 200, body is array containing the created transaction.
       - `GET /api/transactions?month=2026-04` → create transaction with `createdAt: '2026-04-15T10:00:00.000Z'`, expect filtered result.
       - `PATCH /api/transactions/:id` → send `{ amount: 50000, merchant: 'Updated' }` using captured `transactionId`. Expect 200, body has `amount: -50000`, `merchant: 'Updated'`.
       - `DELETE /api/transactions/:id` → delete using `transactionId`. Expect 200, body `{ success: true }`. Then `GET /api/transactions` should not contain the deleted transaction.

       **Insights Endpoint:**
       - `GET /api/insights` → after creating 2 transactions, expect 200, body contains `total`, `transactionCount`, `categoryBreakdown` array, `dailyExpenses` array.

       **Input Endpoints (mocked LLM):**
       - `POST /api/input/text` → send `{ message: 'grab 35k' }`. Expect 200, body contains `amount: 35000`, `category: 'Di chuyển'`. (This uses merchant map fallback, no LLM call.)
       - `POST /api/input/image` → send `{ image: 'data:image/jpeg;base64,abc123' }`. Since LLM is not mocked at the E2E level, this may fail with 502/503. For happy path, mock the OpenAI module at the top of the E2E file with `jest.mock('../src/lib/openrouter', ...)` returning a successful response, OR skip this test and add a comment that it requires LLM mocking. The plan should include the mock.

       **Account Endpoints:**
       - `GET /api/account/export` → after creating transactions, expect 200, body contains `exportedAt`, `transactions` array, `categories` array.
       - `DELETE /api/account` → expect 200, body `{ success: true }`. Then `GET /api/transactions` should return empty array.

    4. For the `POST /api/input/image` test, mock `openai` at the top of the file:
       ```typescript
       jest.mock('../src/lib/openrouter', () => ({
         getOpenAIClient: jest.fn(() => ({
           chat: { completions: { create: jest.fn().mockResolvedValue({ choices: [{ message: { content: '{"amount": 50000, "merchant": "Test", "category": "Ăn uống"}' } }] }) } }
         }))
       }));
       ```

    5. Run `npm run test:e2e` and verify all tests pass.
  </action>
  <verify>
    <automated>npm run test:e2e -- --silent</automated>
  </verify>
  <done>
    - `test/app.e2e-spec.ts` contains E2E tests for all 10+ endpoints
    - `GET /api/health`, `GET /api/categories`, `POST/GET/PATCH/DELETE /api/transactions`, `GET /api/insights`, `POST /api/input/text`, `POST /api/input/image`, `GET /api/account/export`, `DELETE /api/account`
    - Each test creates data, operates on it, and verifies state
    - `npm run test:e2e` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| E2E test → production db | E2E tests use testDb (:memory:) and file:local.db; never Turso |
| mock auth → real auth | AuthGuard is overridden; no real OAuth flows in E2E tests |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Spoofing | Mock session in E2E tests | accept | Tests only; production uses real auth |
| T-04-08 | Information Disclosure | E2E test data in local.db | mitigate | `cleanTestData()` removes records; file is local only |
</threat_model>

<verification>
- `test/helpers/e2e-app.ts` compiles without errors
- `npm run test:e2e` exits 0 with all scenarios passing
</verification>

<success_criteria>
- E2E test helper creates NestJS app with testDb injected and mock auth guard
- All 10+ API endpoints have happy-path E2E scenarios
- Tests verify both request responses and database state changes
- `npm run test:e2e` passes completely
- No external API calls during E2E test execution
</success_criteria>

<output>
After completion, create `.planning/phases/04-automated-quality-gates/04-04-SUMMARY.md`
</output>
