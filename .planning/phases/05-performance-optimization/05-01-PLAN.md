---
phase: 05-performance-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/transactions/transactions.service.ts
  - src/transactions/transactions.controller.ts
  - src/transactions/transactions.service.spec.ts
  - test/app.e2e-spec.ts
autonomous: true
requirements:
  - PERF-01
  - PERF-03
must_haves:
  truths:
    - "GET /api/transactions supports ?page=1&limit=50 and returns paginated shape"
    - "Month filtering uses precise date boundaries (gte/lt) instead of LIKE prefix match"
    - "All existing tests pass with new pagination/filter logic"
  artifacts:
    - path: "src/transactions/transactions.service.ts"
      provides: "Paginated listByMonth with gte/lt filtering"
      contains: "{ data, total, hasMore }"
    - path: "src/transactions/transactions.controller.ts"
      provides: "Page/limit query param parsing"
      contains: "@Query('page')"
    - path: "src/transactions/transactions.service.spec.ts"
      provides: "Unit tests for pagination and filtering"
      contains: "hasMore"
    - path: "test/app.e2e-spec.ts"
      provides: "E2E tests for paginated response"
      contains: "data"
  key_links:
    - from: "src/transactions/transactions.controller.ts"
      to: "src/transactions/transactions.service.ts"
      via: "page/limit params passed to listByMonth"
    - from: "src/transactions/transactions.service.ts"
      to: "test/app.e2e-spec.ts"
      via: "response shape changed from Array to { data, total, hasMore }"
---

<objective>
Add pagination to the transaction list endpoint and replace inefficient LIKE-based month filtering with precise date boundary comparisons.

Purpose: Prevent memory exhaustion and Vercel timeouts when users accumulate hundreds of transactions; eliminate the slow `LIKE 'YYYY-MM%'` pattern that prevents index optimization.
Output: Paginated transaction list with efficient month filtering, updated tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Current Code

From `src/transactions/transactions.service.ts`:
```typescript
async listByMonth(userId: string, month?: string) {
  if (month && !/^\d{4}-\d{2}$/.test(month)) {
    throw new BadRequestException('Invalid month format. Expected YYYY-MM');
  }

  const now = new Date();
  const targetMonth =
    month ||
    `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}`;

  return this.db
    .select()
    .from(transactions)
    .where(
      and(
        eq(transactions.userId, userId),
        like(transactions.createdAt, `${targetMonth}%`),
      ),
    )
    .orderBy(desc(transactions.createdAt));
}
```

From `src/transactions/transactions.controller.ts`:
```typescript
@Get()
async list(@Session() session: UserSession, @Query('month') month?: string) {
  return this.transactionsService.listByMonth(session.user.id, month);
}
```

## Schema Note
`transactions.createdAt` is stored as `text` (ISO 8601 string, e.g. `"2026-04-15T10:00:00.000Z"`). This was intentionally kept as TEXT in Phase 1 (migration to integer deferred to v1.1).

## Required Imports
The service currently imports: `eq, and, like, desc` from `drizzle-orm`. Need to add `gte, lt`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pagination and efficient month filtering in TransactionsService</name>
  <files>src/transactions/transactions.service.ts</files>
  <behavior>
    - Test: listByMonth with no pagination params returns page 1, limit 50 by default
    - Test: listByMonth('user', undefined, 2, 10) returns correct offset (skip 10)
    - Test: hasMore is true when total > page * limit
    - Test: hasMore is false when total <= page * limit
    - Test: month filtering uses gte/lt with correct ISO date boundaries
    - Test: month '2026-04' filters from '2026-04-01T00:00:00.000Z' to '2026-05-01T00:00:00.000Z'
  </behavior>
  <action>
Modify `listByMonth` signature to accept optional pagination params:
`async listByMonth(userId: string, month?: string, page: number = 1, limit: number = 50)`

1. Add `gte` and `lt` imports from `drizzle-orm` (remove `like` if no longer used elsewhere).
2. Validate `page` and `limit`: if provided, ensure they are positive integers. Default to page=1, limit=50. Enforce max limit of 100 to prevent abuse.
3. Compute `offset = (page - 1) * limit`.
4. Replace the `like(transactions.createdAt, `${targetMonth}%`)` condition with explicit date boundaries:
   - `startOfMonth = ${targetMonth}-01T00:00:00.000Z`
   - Compute endOfMonth: create a Date from startOfMonth, add 1 month, format back to ISO string.
   - Use `gte(transactions.createdAt, startOfMonth)` and `lt(transactions.createdAt, endOfMonth)`.
   - NOTE: `createdAt` is stored as TEXT ISO string, so lexicographic comparison works correctly for these boundaries.
5. Execute TWO queries:
   - Count query: `select { count: sql<number>`count(*)` }` with the same WHERE clause to get total.
   - Data query: `.select().from(transactions).where(...).orderBy(desc(transactions.createdAt)).limit(limit).offset(offset)`
6. Return shape:
   ```typescript
   {
     data: Transaction[],
     total: number,
     hasMore: boolean,
   }
   ```
   Where `hasMore = total > page * limit`.

**Important:** The `startOfMonth`/`endOfMonth` computation must handle year boundaries correctly (e.g., month "2026-12" → end is "2027-01-01T00:00:00.000Z").
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=transactions.service.spec.ts</automated>
  </verify>
  <done>
    - listByMonth accepts page/limit with defaults and max limit enforcement
    - Returns { data, total, hasMore } instead of raw array
    - Month filtering uses gte/lt with correct ISO boundaries
    - Unit tests pass covering pagination and boundary logic
  </done>
</task>

<task type="auto">
  <name>Task 2: Update controller and E2E tests for paginated response</name>
  <files>src/transactions/transactions.controller.ts, test/app.e2e-spec.ts</files>
  <action>
1. In `src/transactions/transactions.controller.ts`:
   - Update the `list` method to extract `@Query('page')` and `@Query('limit')` as strings, parse them to integers (using `parseInt` with fallback to undefined), and pass them to `listByMonth`.
   - Import `ParseIntPipe` from `@nestjs/common` or parse manually — prefer manual parsing with validation to default to undefined rather than throwing on missing params.

2. In `test/app.e2e-spec.ts`:
   - Update the `GET /api/transactions → lists transactions` test to expect `response.body.data` as an array instead of `response.body` directly.
   - Update the `DELETE /api/transactions/:id → deletes transaction` test where it lists transactions after deletion: change `listRes.body.find(...)` to `listRes.body.data.find(...)`.
   - Add a new E2E test: `GET /api/transactions?page=1&limit=1 → returns paginated shape with hasMore`.
     - Create 2 transactions, request limit=1, assert `body.data.length === 1`, `body.total === 2`, `body.hasMore === true`.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=transactions.service.spec.ts && npm run test:e2e</automated>
  </verify>
  <done>
    - Controller passes page/limit query params to service
    - E2E tests expect paginated response shape
    - New E2E test verifies pagination behavior
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted page/limit params from query string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | `list` endpoint | mitigate | Enforce max limit of 100 to prevent `limit=999999` memory exhaustion attacks |
| T-05-02 | Tampering | `page`/`limit` params | mitigate | Parse and validate as positive integers; ignore invalid values (default to safe values) |
</threat_model>

<verification>
- `npm test -- --testPathPattern=transactions.service` passes
- `npm run test:e2e` passes
- Manual check: `GET /api/transactions?page=1&limit=2` returns `{ data: [...], total: N, hasMore: boolean }`
</verification>

<success_criteria>
1. `GET /api/transactions` returns `{ data: [...], total: number, hasMore: boolean }` with default page=1, limit=50
2. `GET /api/transactions?page=2&limit=10` returns correct offset data
3. Month filter uses `gte(createdAt, '2026-04-01T00:00:00.000Z')` and `lt(createdAt, '2026-05-01T00:00:00.000Z')` — no `LIKE` in query
4. All unit and E2E tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-optimization/05-01-SUMMARY.md`
</output>
